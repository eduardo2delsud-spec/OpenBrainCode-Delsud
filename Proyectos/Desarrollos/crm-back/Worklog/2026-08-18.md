---
type: worklog
project: crm-back
date: 2026-08-18
tags: [worklog, diagramas, mermaid, documentacion]
updated: 2026-08-18
---

# Worklog — crm-back — 2026-08-18

> Registro **append-only**. Al final de la sesión, si algo cambió que sea *cierto*, **promovelo** a la nota durable y actualizá su `updated`.

## Sesiones

### 13:00 — Diagramas ER — inicio
- **Objetivo:** Agregar diagramas entidad-relación (Mermaid) a la documentación del CRM, en espejo de `gestion-desarrollos-back` (misma tarea, misma decisión: doc aparte `docs/diagrams.md` con columnas completas).
- **Archivos que pienso tocar:** `docs/diagrams.md` (nuevo), `docs/base-de-datos.md`, `docs/README.md`, `CHANGELOG.md`, ficha del proyecto + worklog.

### 13:30 — Diagramas ER — fin
- **Hecho:**
  - Leí los 25 modelos de `src/models/` + relaciones de `src/db/connection.js` + los 4 modelos de `src/db/gestion-db.js` (fuente canónica del esquema CRM).
  - Creé `docs/diagrams.md` con 3 bloques Mermaid: (1) ER DB CRM principal — 25 tablas con columnas completas + pivotes auto `ContactZone`/`DevelopmentAmenities`, relaciones de `connection.js` y nota de modelos sin asociaciones (`Tags`, `TargetMonth`, `TypeDoc`, `Documentation`, `OrderAdvisors`); (2) ER DB Gestión — 4 modelos mínimos (`clients`, `bookings`, `client_documents`, `users`) con nota de que el esquema completo vive en gestion-back; (3) flowchart puente CRM↔Gestión (`syncBookingOnCreation`: `contact_id`, `crm_booking_id`, `user_id = 1`).
  - Enlacé el doc desde `docs/base-de-datos.md` (cabecera) y `docs/README.md` (índice); entrada Maintenance en `CHANGELOG.md`.
  - Actualicé la ficha del proyecto en el vault + worklog.
  - **Pendiente:** validar los 3 bloques Mermaid con `check-mermaid-browser.cjs` (misma técnica que en gestion-back).
- **Bloqueos / decisiones pendientes:** Ninguno.

## Changelog (qué cambió de verdad)

- `13:10` — Creación de `docs/diagrams.md` (3 diagramas Mermaid) — archivo: `docs/diagrams.md`.
- `13:15` — Enlaces en `docs/base-de-datos.md` (cabecera) y `docs/README.md` (índice).
- `13:20` — Entrada Maintenance en `CHANGELOG.md`.

## Durable a promover

| Qué cambió | Nota a actualizar |
|------------|-------------------|
| Se agregaron los diagramas entidad-relación (Mermaid) de ambas bases y del puente de sincronización en `docs/diagrams.md`. | Ficha `Proyectos/Desarrollos/crm-back/crm-back.md` (Documentación, Historial) — `updated: 2026-08-18`. |

## Bloqueadores / trabas

- Ninguno.

## Sesión extra — Auditoría de hallazgos + ADR-007 (registrada el 2026-08-19 desde el CHANGELOG del repo)

> Append al worklog: el 08-18 no fue solo diagramas. El CHANGELOG del repo registra además lo siguiente, que se documentó en el vault recién el 19/8:

- **Fixes de auditoría (hallazgos MEDIO y BAJO):** búsqueda por zona rota (500 siempre), estados de reserva sin whitelist (se podía reconcretar una "Concretada"), datos de vehículo pegados en la fila, listados de reservas que perdían limit/offset, SVG fuera de mimetypes de uploads, galería de desarrollos que no validaba imágenes, guardas faltantes (zona inexistente, `lots` no-array, `advisor.fullName` null), columna inexistente en `getAllDataLot`, argb de 6 dígitos en ExcelJS, alineación "En Vigencia"/"En vigencia".
- **`updateClient` con transacción** (hallazgo 33): contacto + historial en `conn.transaction`.
- **Cabeceras de seguridad removidas** (decisión de viku, 2026-08-18): CSP, `X-Frame-Options`, `X-Content-Type-Options` fuera de `src/app.js`. **No volver a proponerlo.**
- **Asimetría de paginado documentada** (hallazgo 28) + hallazgo 48 revisado sin tocar.
- **ADR-007** (`docs/adr/ADR-007-normalizacion-del-esquema-de-reservas.md`) + catálogos de endpoints en `docs/api-*-endpoints.md` + `docs/auditoria-hallazgos-2026-08-18.md`.