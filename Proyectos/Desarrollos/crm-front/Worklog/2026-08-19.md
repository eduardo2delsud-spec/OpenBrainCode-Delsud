---
type: worklog
project: crm-front
date: 2026-08-19
tags: [worklog, documentNumber, migracion, reestructuracion]
updated: 2026-08-19
---

# Worklog — crm-front — 2026-08-19

> Registro **append-only**. Al final de la sesión, si algo cambió que sea *cierto*, **promovelo** a la nota durable y actualizá su `updated`. Sesión registrada el mismo día desde el CHANGELOG del repo (commit `f314c03`).

## Sesiones

### Migración `numberId` → `documentNumber` — 2026-08-19
- **El campo del documento del contacto deja de llamarse `numberId`** en formularios, payloads y lecturas de la API: formulario del firmante 1 del wizard de reserva (`StepOne`), alta/edición de contacto (`ModalCreateContact`, `ContactProfileDetails`), lecturas del detalle de reserva (`Ficha`) y del contacto, PDFs de reserva (`PDFBookingContado/Financiado`, `PDFComponent`, `generateReservationReceiptPDF`), helper `formatDocumentNumber` y el selector del test de Playwright del flujo de reserva.
- **`numberId2` (firmante 2) no se tocó:** sigue siendo `Bookings.numberId2` y se resolverá con la reestructuración de esa tabla (ver ADR-007 de crm-back).
- **Reestructuración de `src/`** (commit `270927e`): `App.jsx`/`main.jsx`/`index.css` a `src/app/`, `src/api/` → `src/shared/api/`, config de Playwright y auditoría de hallazgos en la raíz.

## Changelog (qué cambió de verdad)

- Migración `numberId` → `documentNumber` en 15 archivos (forms, payloads, lecturas, PDFs, tests) — 2026-08-19.
- Reestructuración `src/` (app/, features/, shared/api) — 2026-08-19.

## Durable a promover

| Qué cambió | Nota a actualizar |
|------------|-------------------|
| El front migró a `documentNumber` (alineado con crm-back); se reestructuró `src/`. | Ficha `Proyectos/Desarrollos/crm-front/crm-front.md` (Estado, Documentación, Historial) — `updated: 2026-08-19`. |

## Bloqueadores / trabas

- Ninguno.