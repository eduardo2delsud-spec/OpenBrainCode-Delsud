---
type: worklog
project: gestion-desarrollos-back
date: 2026-08-18
tags: [worklog, env, config, centralizacion]
updated: 2026-08-18
---

# Worklog — gestion-desarrollos-back — 2026-08-18

> Registro **append-only**. Al final de la sesión, si algo cambió que sea *cierto*, **promovelo** a la nota durable y actualizá su `updated`.

## Sesiones

### 12:30 — Estandarización de variables de entorno (backend) — inicio
- **Objetivo:** Relevar y estandarizar las variables de entorno: eliminar obsoletas, documentar las que el código usa, hacer opcional AWS en dev y centralizar el acceso a `process.env` en `config`.
- **Archivos que pienso tocar:** `.env.example`, `.env`, `src/core/server/config.ts`, `src/shared/types/connection.ts`, servicios que leían `process.env`, `docs/entorno.md`, `CHANGELOG.md`.

### 13:00 — Estandarización de variables de entorno (backend) — fin
- **Diagnóstico del relevamiento:**
  - **Obsoletas sin uso en código:** `SECRET_JWT_KEY_REFRESH`, `CRM_API_URL`, `ADMIN_EMAIL`, `EMAIL_SISTEMAS` (esta última con valor roto `eduardo.` en `.env`).
  - **Faltantes (usadas pero no declaradas/documentadas):** `SYSTEM_USER_PASSWORD` (seeder `db:seed:primary`), `LOG_LEVEL` (winston), `BCC_EMAIL` (sendEmail), `SENDGRID_API_KEY` (estaba comentada con una key real en `.env` local).
  - `config.ts` agrega `_dev/_test/_prod` a `DB_NAME` pero no a `CRM_DB_NAME` (comportamiento correcto, se documentó). `AWS_*` eran requeridas por Joi → el backend no arrancaba en dev sin credenciales S3.
  - 19 lecturas de `process.env.AWS_BUCKET_URL` dispersas en 5 services + `process.env.NODE_ENV` en `errorHandler.ts`/`response.ts`.
- **Hecho:**
  - `.env.example` reescrito: quitadas `CRM_API_URL`/`EMAIL_SISTEMAS`; agregadas `SYSTEM_USER_PASSWORD`, `BCC_EMAIL`, `LOG_LEVEL`; comentarios de sufijo DB y AWS opcional en dev.
  - `config.ts` (Joi): `AWS_*` con `.when("NODE_ENV", { is: "development", then: optional, otherwise: required })`; opcionales nuevas `LOG_LEVEL`, `SENDGRID_API_KEY`, `FROM_EMAIL`, `BCC_EMAIL`, `EMAIL_DESARROLLOS_LAPLATA`, `EMAIL_DESARROLLOS_MENDOZA`; `config` expone `logLevel`, `sendgridApiKey`, `fromEmail`, `bccEmail`, `emailLaPlata`, `emailMendoza`. `AppConfig` tipado en `src/shared/types/connection.ts`.
  - Centralización: `logger.ts` → `config.logLevel`; `sendEmail.ts` → `config.sendgridApiKey/fromEmail/bccEmail`; `email.service.ts`/`receiptEmail.service.ts` → `config.emailMendoza/emailLaPlata`; Claims/Contracts/Downpayments/Reservations/Clients → `config.awsBucketUrl` (default `""`); `errorHandler.ts`/`response.ts` → `config.env`. Tras esto solo `config.ts` lee `process.env` (verificado por grep).
  - `.env` local limpio (obsoletas eliminadas, SENDGRID vacía, agregadas `LOG_LEVEL`/`BCC_EMAIL`). Nota: se conservaron los valores locales reales.
  - Docs: tabla de variables + matriz por ambiente en `docs/entorno.md`.
  - Validación: `npm run typecheck` (exit 0), `npm run build` (exit 0), boot del dist **sin credenciales AWS** (en `:4099`): arranca, conecta DBs, `/api/v1/health` → 200 `ok`. Biome: 5 errores preexistentes (seeders + certifiedtickets), ninguno en archivos tocados.
- **Acierto:** Centralizar env en `config` (Joi fail-fast) + AWS opcional en dev permite arrancar local sin credenciales; se documentó la matriz por ambiente. Registrado en `Brain/Aciertos/`.
- **Bloqueos / decisiones pendientes:** El commit del backend (incluidos estos cambios) lo hizo el usuario o un proceso externo (`f017e3c`, autor `eduardodelsud-web`) durante la sesión; no fue el plugin `vault-sync` (solo cubre el vault). Contenido verificado: correcto, sin secretos.

## Changelog (qué cambió de verdad)

- `13:00` — Estandarización de variables de entorno — `.env.example`/`.env` (limpiados); `src/core/server/config.ts` (AWS opcional en dev + vars opcionales); `src/shared/types/connection.ts` (AppConfig extendido); `logger.ts`, `sendEmail.ts`, `errorHandler.ts`, `response.ts`, `email.service.ts`, `receiptEmail.service.ts`, `claims.service.ts`, `contracts.service.ts`, `downpayments.service.ts`, `reservations.service.ts`, `clients.service.ts` (centralizados a `config`); `docs/entorno.md` (tabla + matriz); entrada Changed en `CHANGELOG.md`.

## Durable a promover

| Qué cambió | Nota a actualizar |
|------------|-------------------|
| Variables de entorno estandarizadas (AWS opcional en dev, obsoletas eliminadas, faltantes documentadas, acceso centralizado en `config`, matriz por ambiente en `docs/entorno.md`). | Ficha `Proyectos/Desarrollos/gestion-desarrollos-back/gestion-desarrollos-back.md` (Estado, Documentación, Historial, Patrones) — `updated: 2026-08-18`. |

## Bloqueadores / trabas

- Ninguno (el commit externo se verificó y es correcto).
