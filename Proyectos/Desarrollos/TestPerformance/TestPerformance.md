---
type: proyecto
project: TestPerformance
path: C:\Users\edu\Desktop\DelSud\Desarrollos\TestPerformance
stack: ["@sendgrid/mail", "aws-sdk", "axios", "bcryptjs", "cookie-parser", "cors", "dayjs", "dotenv", "drizzle-orm", "exceljs", "express", "husky", "joi", "jsonwebtoken", "morgan", "mysql2", "node-cron", "winston"]
arch: simple
dominio: indefinido
updated: 2026-08-12
---

# TestPerformance

> 

## Qué hace

- (describir en una línea; lo completa el skill)

## Estado actual

- (estado actual: en desarrollo / mantenido / archivo)

<!-- AUTO: cuerpo factual generado por generar-ficha.ps1 (no editar) -->
## Stack

| Capa | Tecnología | Detalle |
|------|-----------|---------|
| Runtime | @sendgrid/mail, aws-sdk, axios, bcryptjs, cookie-parser, cors, dayjs, dotenv, drizzle-orm, exceljs, express, husky, joi, jsonwebtoken, morgan, mysql2, node-cron, winston | dependencies |
| Dev | @biomejs/biome, @types/cookie-parser, @types/cors, @types/exceljs, @types/express, @types/joi, @types/jsonwebtoken, @types/morgan, @types/node, @types/node-cron, drizzle-kit, esbuild, tsc-alias, tsx, typescript | devDependencies |

## Comandos útiles

```bash
build
build:js
check
copy:assets
crm:generate
crm:pull
crm:push
db:generate
db:migrate
db:pull
db:push
db:seed
db:seed:cash-flow
db:seed:docslots
db:seed:docslots:generic
db:seed:primary
db:seed:test-contracts
db:studio
db:up
dev
format
format:check
lint
lint:fix
start
typecheck
```

## Arquitectura

```
workspace: none
arch: simple
manifests:
  api-old\package.json
top_folders:
  api-old
```

## Servicios y puertos

- (sin docker-compose detectado)

## Agentes opencode

- (sin agentes opencode)
<!-- /AUTO -->


## Conceptos que usa

- (por completar)

## Patrones que sigue

- (por completar)

## Decisiones clave

- (por completar)

## Lecciones

- (por completar)

## Dónde buscar más

- (por completar)

## Historial (worklog)

- (por completar)
