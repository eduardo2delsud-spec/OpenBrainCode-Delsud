---
type: worklog
project: gestion-desarrollos
date: 2026-08-18
tags: [worklog, limpieza, dead-code, dependencias]
updated: 2026-08-18
---

# Worklog — gestion-desarrollos — 2026-08-18

> Registro **append-only**. Al final de la sesión, si algo cambió que sea *cierto*, **promovelo** a la nota durable y actualizá su `updated`.

## Sesiones

### 10:00 — Limpieza de dead code (frontend) — inicio
- **Objetivo:** Implementar el plan de depuración del frontend: eliminar archivos muertos, barrels huérfanos, assets sin uso, dependencias sin uso y corregir un hook con ruta inexistente.
- **Archivos que pienso tocar:** `src/` (eliminaciones), `public/` (eliminaciones), `package.json`, `package-lock.json`, `docs/routing.md`, `CHANGELOG.md`, ficha del proyecto + worklog.

### 10:30 — Verificación a fondo del plan — antes de tocar código
- **Hecho:**
  - Verifiqué cada afirmación del plan contra el código real (importadores, barrels, lockfile, rutas del backend).
  - Detecté que el plan estaba **stale** en dependencias: `@babel/plugin-proposal-private-property-in-object` y `source-map-explorer` **ya se habían eliminado** el 2026-08-14 (CHANGELOG); no estaban en `package.json` ni `package-lock.json`.
  - Detecté que eliminar `@mui/system` contradecía una **decisión documentada del equipo** (CHANGELOG 2026-08-14: "se mantiene explícita por decisión del equipo"). El usuario ratificó su eliminación.
  - Corregí la clasificación del plan: `components/Lists/index.js` y `components/ReservasTable/index.js` también son barrels huérfanos (los componentes se importan por archivo directo; el barrel no). Quedaron fuera de alcance por decisión del plan.
  - Verifiqué que los 13 errores de lint son preexistentes y ajenos al cambio.

### 11:00 — Limpieza de dead code (frontend) — fin
- **Hecho:**
  - Eliminé 11 archivos muertos: `src/hooks/useDebounce.js`, `src/components/PerfilPersonal/ListTopClients.jsx`, `src/components/Dialogs/WarningDialog.jsx` + `DuplicateDataDialog.jsx`, `src/pages/ErrorPage.jsx`, `src/assets/pdfAssets/LaSerenaLogoPDF.jsx` + `LaSerenaLogoPrint.jsx` + `LaSerenaWatermarkLogo.jsx`, `src/assets/svgComponents/LogoDelsud.jsx`, `src/assets/avatars/desa.png` + `logo.svg`.
  - Eliminé 3 barrels huérfanos: `src/pages/index.js`, `src/components/Dialogs/index.js`, `src/assets/avatars/index.js`.
  - Eliminé 4 assets de `public/` sin uso: `comprobante_test.png`, `laplata_vista_aerea.webp`, `marker-icon.png`, `marker-shadow.png`.
  - `npm uninstall @mui/system` (0 imports directos; queda como transitiva de `@mui/material`) + `npm install -D globals@^14.0.0` (lo usa `eslint.config.mjs` y se resolvía por hoisting).
  - Quité el bloque legacy `eslintConfig` de `package.json` (ignorado por el flat config de ESLint 9).
  - Eliminé el hook sin uso `useClientDebtSummary` de `src/hooks/queries/clients-cobranza.js` (llamaba a `GET /clients/cobranza/debt-summary/:clientId`, ruta inexistente; la correcta es `GET /clients/:id/debt-summary`). El archivo conserva `COBRANZA_KEYS`, usado por 4 dominios de queries.
  - Actualicé `docs/routing.md` (ruta `*` → NoMatch, se eliminó la referencia a ErrorPage) y agregué entrada Maintenance al `CHANGELOG.md`.
  - Validé: `npm run build` OK (1790 módulos) y `npm run lint` sin errores nuevos (13 errores preexistentes ajenos al cambio).
- **Bloqueos / decisiones pendientes:** Ninguno.

## Changelog (qué cambió de verdad)

- `11:00` — Limpieza de dead code del frontend — archivos: 11 archivos muertos + 3 barrels + 4 assets de `public/` eliminados; `package.json`/`package-lock.json` (removido `@mui/system`, `eslintConfig`; agregado `globals`); `src/hooks/queries/clients-cobranza.js` (eliminado `useClientDebtSummary`); `docs/routing.md`; entrada Maintenance en `CHANGELOG.md`.

## Durable a promover

| Qué cambió | Nota a actualizar |
|------------|-------------------|
| Limpieza de dead code y dependencias del frontend (archivos muertos, barrels huérfanos, assets sin uso, `@mui/system` removido, `globals` agregado, hook con ruta inexistente eliminado). | Ficha `Proyectos/Desarrollos/gestion-desarrollos/gestion-desarrollos.md` (Historial) — `updated: 2026-08-18`. |

## Bloqueadores / trabas

- Ninguno.