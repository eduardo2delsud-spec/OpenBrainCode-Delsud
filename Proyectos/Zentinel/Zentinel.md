---
type: proyecto
project: Zentinel
path: C:\Users\edu\Desktop\DelSud\Zentinel
stack: ["@types/better-sqlite3", "@types/morgan", "@types/node-cron", "axios", "better-sqlite3", "cors", "dotenv", "drizzle-kit", "drizzle-orm", "express", "ignore", "lucide-react", "mermaid", "morgan", "node-cron", "react", "react-dom", "react-is", "react-markdown", "react-router-dom", "react-syntax-highlighter", "recharts", "remark-gfm", "socket.io", "socket.io-client"]
arch: monorepo
dominio: indefinido
updated: 2026-08-12
---

# Zentinel

> 

## Qué hace

- (describir en una línea; lo completa el skill)

## Estado actual

- (estado actual: en desarrollo / mantenido / archivo)

<!-- AUTO: cuerpo factual generado por generar-ficha.ps1 (no editar) -->
## Stack

| Capa | Tecnología | Detalle |
|------|-----------|---------|
| Runtime | @types/better-sqlite3, @types/morgan, @types/node-cron, axios, better-sqlite3, cors, dotenv, drizzle-kit, drizzle-orm, express, ignore, lucide-react, mermaid, morgan, node-cron, react, react-dom, react-is, react-markdown, react-router-dom, react-syntax-highlighter, recharts, remark-gfm, socket.io, socket.io-client | dependencies |
| Dev | @eslint/js, @types/cors, @types/express, @types/node, @types/react, @types/react-dom, @types/react-syntax-highlighter, @vitejs/plugin-react, eslint, eslint-plugin-react-hooks, eslint-plugin-react-refresh, globals, ts-node-dev, tsx, typescript, typescript-eslint, vite | devDependencies |

## Comandos útiles

```bash
build
db:push
dev
lint
preview
start
```

## Arquitectura

```
workspace: npm
arch: monorepo
manifests:
  apps\backend\package.json
  apps\frontend\package.json
top_folders:
  apps
  tmp
```

## Servicios y puertos

- (sin docker-compose detectado)

## Agentes opencode

- (sin agentes opencode)
<!-- /AUTO -->


## Conceptos que usa

- (por completar)

## Patrones que sigue

- (por completar)

## Decisiones clave

- (por completar)

## Lecciones

- (por completar)

## Dónde buscar más

- (por completar)

## Historial (worklog)

- (por completar)
